node_modules
.next
.env*.local
.DS_Store
*.log

# Local-only project context files (notes, reference scripts)
initial context import files/
docs/ORIGIN_DAYDREAM_GLYPHS.md

# Syncthing machine-local files (not synced between devices)
.stfolder/
.stignore

# Claude Code per-machine permission allowlist (local only)
.claude/settings.local.json
